The source script is built to generate a machine to produce univariates. The source file has to be input as a CSV and the output produced from the gui.py is an XL; it should also produce a word file but that function is not working.

An updated script will produce PPTs with univariates.
